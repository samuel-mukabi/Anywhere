{
  "name": "@anywhere/workers",
  "version": "1.0.0",
  "description": "Background jobs encompassing MongoDB catalog synchronization and nightly maintenance",
  "main": "dist/cron.js",
  "scripts": {
    "build": "tsc",
    "seed": "ts-node src/seed-destinations.ts",
    "start": "node dist/cron.js",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anywhere/api-clients": "*",
    "@repo/types": "*",
    "csv-parse": "^6.2.1",
    "dotenv": "^16.4.5",
    "mongoose": "^8.3.0",
    "node-cron": "^3.0.3",
    "pino": "^9.5.0",
    "pino-pretty": "^11.2.2"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
